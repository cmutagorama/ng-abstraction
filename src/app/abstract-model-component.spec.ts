import { AbstractModelComponent } from './abstract-model-component';

describe('AbstractModelComponent', () => {
  it('should create an instance', () => {
    expect(new AbstractModelComponent()).toBeTruthy();
  });
});
